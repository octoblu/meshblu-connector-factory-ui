import React, { PropTypes } from 'react'

const propTypes = {}
const defaultProps = {}

const OsArchButton = () => {
  return <div>OsArchButton</div>
}

OsArchButton.propTypes    = propTypes
OsArchButton.defaultProps = defaultProps

export default OsArchButton
